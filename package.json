{
    "name": "visits-counter",
    "scripts": {
        "postinstall": "cd react-app && npm install && npm run build --f",
        "server": "node index.js"
    },
    "dependencies": {
        "dotenv": "^16.3.1",
        "express": "^4.18.1",
        "mongoose": "^6.4.6"
    }
}
